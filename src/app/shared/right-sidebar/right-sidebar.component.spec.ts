// // This shows a different way of testing a component, check about for a simpler one
// import { Component } from '@angular/core';

// import { TestBed } from '@angular/core/testing';

// import { RightSidebarComponent } from './right-sidebar.component';

// describe('RightSidebar Component', () => {
//   const html = '<right-sidebar></right-sidebar>';

//   beforeEach(() => {
//     TestBed.configureTestingModule({declarations: [RightSidebarComponent, TestComponent]});
//     TestBed.overrideComponent(TestComponent, { set: { template: html }});
//   });

//   it('should ...', () => {
//     const fixture = TestBed.createComponent(TestComponent);
//     fixture.detectChanges();
//     expect(fixture.nativeElement.children[0].textContent).toContain('RightSidebar Works!');
//   });

// });

// @Component({selector: 'my-test', template: ''})
// class TestComponent { }
