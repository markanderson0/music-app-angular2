// // This shows a different way of testing a component, check about for a simpler one
// import { Component } from '@angular/core';

// import { TestBed } from '@angular/core/testing';

// import { MainSidebarComponent } from './main-sidebar.component';

// describe('MainSidebar Component', () => {
//   const html = '<main-sidebar></main-sidebar>';

//   beforeEach(() => {
//     TestBed.configureTestingModule({declarations: [MainSidebarComponent, TestComponent]});
//     TestBed.overrideComponent(TestComponent, { set: { template: html }});
//   });

//   it('should ...', () => {
//     const fixture = TestBed.createComponent(TestComponent);
//     fixture.detectChanges();
//     expect(fixture.nativeElement.children[0].textContent).toContain('MainSidebar Works!');
//   });

// });

// @Component({selector: 'my-test', template: ''})
// class TestComponent { }
